{
  "name": "internal-operations-platform",
  "version": "1.0.0",
  "description": "Unified backend API platform for managing multiple internal business processes",
  "main": "src/app.js",
  "type": "module",
  "scripts": {
    "start": "npx prisma migrate deploy && node src/app.js",
    "dev": "nodemon src/app.js",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:seed": "node prisma/seed.js",
    "db:studio": "prisma studio",
    "export:makes": "node prisma/export-makes-models-parts.js",
    "deploy": "(npx prisma migrate resolve --rolled-back 20260124000000_add_branch_to_user 2>/dev/null || true) && (npx prisma migrate resolve --rolled-back 20260125000000_replace_customer_reference_with_order_date 2>/dev/null || true) && npx prisma migrate deploy && npm run db:seed && npm start"
  },
  "dependencies": {
    "@prisma/client": "^5.10.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "exceljs": "^4.4.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.3",
    "prisma": "^5.10.0"
  }
}
