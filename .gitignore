# Root repo — frontend lives alongside backend
frontend/node_modules/
frontend/dist/

# Local Vite env (VITE_API_URL, etc.)
frontend/.env.local
frontend/.env.*.local

# Server-only: VITE_API_URL for deploy.sh
deploy.env
