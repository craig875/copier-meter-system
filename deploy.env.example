# Copy to deploy.env on the server (same folder as this repo root). Not committed.
# Used by scripts/deploy.sh when building the frontend.

# Public origin of your API (no trailing slash, no /api — Vite appends /api)
VITE_API_URL=https://your-domain.com

# Optional: branch to deploy (default: main)
# DEPLOY_BRANCH=main
