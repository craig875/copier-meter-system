# Backend API URL — required for production builds when frontend and API have different origins.
# Local `npm run dev`: axios uses /api on the Vite dev server, which proxies to localhost:3001
# (see vite.config.js), so a wrong machine-wide VITE_API_URL does not affect dev.
#
# VITE_API_URL=https://your-backend-url.example.com
